<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyKotlinApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- STATUS BAR -->
        <item name="android:statusBarColor">#FA3939</item>
        <item name="android:navigationBarColor">#FA3939</item>

        <!-- Light icons on dark bar -->
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.MyKotlinApp" parent="Base.Theme.MyKotlinApp" />
</resources>